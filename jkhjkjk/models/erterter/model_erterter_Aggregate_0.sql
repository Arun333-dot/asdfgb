{{
  config({    
    "materialized": "table",
    "alias": "prophecy__temp_erterter_post_Aggregate_0_0",
    "database": "hive_metastore",
    "schema": "arun123"
  })
}}

WITH Aggregate_0 AS (

  SELECT * 
  
  FROM `` AS in0

)

SELECT *

FROM Aggregate_0
