{{
  config({    
    "materialized": "table",
    "alias": "prophecy__temp_p77_post_Aggregate_3_0",
    "database": "hive_metastore",
    "schema": "default"
  })
}}

WITH Aggregate_3 AS (

  SELECT * 
  
  FROM `` AS in0

)

SELECT *

FROM Aggregate_3
