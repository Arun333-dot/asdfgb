{{
  config({    
    "materialized": "table",
    "alias": "prophecy__temp_p17_post_Filter_2_0",
    "database": "hive_metastore",
    "schema": "arun123"
  })
}}

WITH Filter_2 AS (

  SELECT * 
  
  FROM `` AS in0
  
  WHERE true

)

SELECT *

FROM Filter_2
