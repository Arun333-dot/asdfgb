{{
  config({    
    "materialized": "table",
    "alias": "prophecy__temp_p17_post_Filter_1_0",
    "database": "hive_metastore",
    "schema": "default"
  })
}}

WITH Filter_1 AS (

  SELECT * 
  
  FROM `` AS in0
  
  WHERE true

)

SELECT *

FROM Filter_1
