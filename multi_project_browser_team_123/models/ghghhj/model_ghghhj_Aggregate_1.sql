{{
  config({    
    "materialized": "table",
    "alias": "prophecy__temp_ghghhj_post_Aggregate_1_0",
    "database": "hive_metastore",
    "schema": "default"
  })
}}

WITH Aggregate_1 AS (

  SELECT * 
  
  FROM `` AS in0

)

SELECT *

FROM Aggregate_1
