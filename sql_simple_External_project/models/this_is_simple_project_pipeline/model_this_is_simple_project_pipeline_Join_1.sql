{{
  config({    
    "materialized": "table",
    "alias": "prophecy__temp_this_is_simple_project_pipeline_post_Join_1_0",
    "database": "database",
    "schema": "default"
  })
}}

WITH Join_1 AS (

  SELECT * 
  
  FROM `` AS in0
  INNER JOIN `` AS in1

)

SELECT *

FROM Join_1
