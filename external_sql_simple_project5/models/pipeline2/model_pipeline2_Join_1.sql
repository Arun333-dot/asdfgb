{{
  config({    
    "materialized": "table",
    "alias": "prophecy__temp_pipeline2_post_Join_1_0",
    "database": "hive_metastore",
    "schema": "arun123"
  })
}}

WITH Join_1 AS (

  SELECT * 
  
  FROM `` AS in0
  INNER JOIN `` AS in1

)

SELECT *

FROM Join_1
