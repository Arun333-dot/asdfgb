{{
  config({    
    "materialized": "table",
    "alias": "prophecy__temp_pipeline3_post_Aggregate_1_0",
    "database": "hive_metastore",
    "schema": "arun123"
  })
}}

WITH Aggregate_1 AS (

  SELECT * 
  
  FROM `` AS in0

)

SELECT *

FROM Aggregate_1
