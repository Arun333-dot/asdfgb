{{
  config({    
    "materialized": "table",
    "alias": "prophecy__temp_asasdasd_post_Filter_2_0",
    "database": "database",
    "schema": "default"
  })
}}

WITH Filter_2 AS (

  SELECT * 
  
  FROM `` AS in0
  
  WHERE true

)

SELECT *

FROM Filter_2
