{{
  config({    
    "materialized": "table",
    "alias": "prophecy__temp_asasdasd_post_Filter_1_0",
    "database": "database",
    "schema": "default"
  })
}}

WITH Filter_1 AS (

  SELECT * 
  
  FROM `` AS in0
  
  WHERE true

)

SELECT *

FROM Filter_1
