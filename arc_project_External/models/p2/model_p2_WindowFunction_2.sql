{{
  config({    
    "materialized": "table",
    "alias": "prophecy__temp_p2_post_WindowFunction_2_0",
    "database": "database",
    "schema": "default"
  })
}}

WITH WindowFunction_2 AS (

  SELECT * 
  
  FROM `` AS in0

)

SELECT *

FROM WindowFunction_2
