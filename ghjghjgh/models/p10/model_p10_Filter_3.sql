{{
  config({    
    "materialized": "table",
    "alias": "prophecy__temp_p10_post_Filter_3_0",
    "database": "hive_metastore",
    "schema": "arun123"
  })
}}

WITH Filter_3 AS (

  SELECT * 
  
  FROM `` AS in0
  
  WHERE true

)

SELECT *

FROM Filter_3
