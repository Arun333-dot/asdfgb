{{
  config({    
    "materialized": "table",
    "alias": "prophecy__temp_p10_post_Join_2_0",
    "database": "hive_metastore",
    "schema": "arun123"
  })
}}

WITH Join_2 AS (

  SELECT * 
  
  FROM `` AS in0
  INNER JOIN `` AS in1

)

SELECT *

FROM Join_2
