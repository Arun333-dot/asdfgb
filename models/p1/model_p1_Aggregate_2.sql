{{
  config({    
    "materialized": "table",
    "alias": "prophecy__temp_p1_post_Aggregate_2_0",
    "database": "database",
    "schema": "default"
  })
}}

WITH Aggregate_2 AS (

  SELECT * 
  
  FROM `` AS in0

)

SELECT *

FROM Aggregate_2
