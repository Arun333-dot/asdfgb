{{
  config({    
    "materialized": "table",
    "alias": "prophecy__temp_p7_post_Aggregate_0_0",
    "database": "database",
    "schema": "default"
  })
}}

WITH Aggregate_0 AS (

  SELECT * 
  
  FROM `` AS in0

)

SELECT *

FROM Aggregate_0
