{{
  config({    
    "materialized": "table",
    "alias": "prophecy__temp_p10_post_Filter_0_0",
    "database": "database",
    "schema": "default"
  })
}}

WITH Filter_0 AS (

  SELECT * 
  
  FROM `` AS in0
  
  WHERE true

)

SELECT *

FROM Filter_0
